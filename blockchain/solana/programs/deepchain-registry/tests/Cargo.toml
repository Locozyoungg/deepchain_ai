# Cargo.toml
[package]
name = "deepchain-registry"
version = "0.1.0"
description = "Decentralized AI Model Registry on Solana"
edition = "2021"

[dependencies]
anchor-lang = "0.26.0"
arrayref = "0.3.6"

[features]
no-entrypoint = []
no-idl = []
cpi = ["no-entrypoint"]